import { makeStyles } from '@mui/styles';

export const useStyle = makeStyles({
    heading: {
        marginBottom: 30,
    },
    main: {
        maxWidth: '100vw',
        marginTop: 15,
        marginLeft: 240,
    },
    inner: {
        margin: '0 50px',
    }
});